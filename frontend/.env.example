VITE_API_URL=http://localhost:8001/api

# Observabilidade (opcional). Se vazio, o Sentry não é carregado.
VITE_SENTRY_DSN=

# Telas de vitrine sem backend real (chat, clube, pagamento, acompanhar, gastos,
# laudo de inspeção). Não definido = liga em dev, desliga em produção.
# Force com true/false. Em produção deixe desligado (não setar ou =false).
# VITE_SHOWCASE=true
